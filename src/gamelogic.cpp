#include <conio.h>
#include "grid.h"
#include "draw.h"

/*
Screen screen;
GameObject player;
Tile playerTile;
bool win;
bool restartlvl;

void LevelLogic()
{
	while (!win)
	{
		switch (getch())
		{
		case 'z':
			screen.MoveVertical(playerTile, -1);
		case 'q':
			screen.MoveHorizontal(playerTile, -1);
		case 's':
			screen.MoveVertical(playerTile, 1);
		case 'd':
			screen.MoveHorizontal(playerTile, 1);
		case 'r':
			restartlvl = true;
			break;
		default:
			//donothing
		}
	}

	if (restartlvl)
	{
		restartlvl = false;
		//we restart the lvl
	}

	if (win)
	{
		//put a win message than go back to lvl selection
	}
}
*/